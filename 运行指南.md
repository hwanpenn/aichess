# aichess 项目运行指南

## 一、环境要求

### 硬件要求
- **必须使用 NVIDIA GPU**（英伟达显卡）
- 蒙特卡洛树搜索需要大量神经网络推理，CPU 无法满足性能要求

### 软件要求
- Python 3.x
- 根据选择的框架安装以下之一：
  - **PyTorch**（推荐）：需要安装 GPU 版本的 PyTorch
  - **PaddlePaddle**：需要安装 GPU 版本的 PaddlePaddle
- 其他依赖库：
  - `numpy`
  - `pygame`（用于 GUI 界面）
  - `redis`（可选，用于分布式数据存储）

## 二、安装步骤

### 0. 创建虚拟环境（强烈推荐）

**为什么要使用虚拟环境？**
- 避免与系统 Python 环境冲突
- 便于管理项目依赖
- 可以创建多个独立的环境用于不同项目

**创建虚拟环境：**

```bash
# Windows (使用 Git Bash 或 PowerShell)
cd C:\CodeHome\aichess
python -m venv venv

# 激活虚拟环境
# Git Bash:
source venv/Scripts/activate
# PowerShell:
venv\Scripts\Activate.ps1
# CMD:
venv\Scripts\activate.bat
```

**激活后，命令行提示符前会显示 `(venv)`**

**退出虚拟环境：**
```bash
deactivate
```

**注意：** 每次运行项目前都需要激活虚拟环境！

### 1. 检查 CUDA 版本（重要）

如果你已经安装了 CUDA，需要先确认版本，以便安装匹配的 PyTorch/PaddlePaddle：

**方法一：使用检查脚本（推荐）**
```bash
cd C:\CodeHome\aichess
python check_env.py
```

**方法二：手动检查**
```bash
# 检查 NVIDIA 驱动和 CUDA
nvidia-smi

# 或者检查 PyTorch 是否能识别 CUDA
python -c "import torch; print('CUDA available:', torch.cuda.is_available()); print('CUDA version:', torch.version.cuda if torch.cuda.is_available() else 'N/A')"
```

**根据 CUDA 版本选择对应的 PyTorch：**
- CUDA 11.8 → PyTorch cu118
- CUDA 12.1 → PyTorch cu121
- 如果没有 CUDA 或版本不匹配，需要先安装对应版本的 CUDA

### 2. 安装 PyTorch（如果选择 PyTorch 框架）

```bash
# CUDA 11.8 版本
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# 或者 CUDA 12.1 版本
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

### 3. 安装 PaddlePaddle（如果选择 PaddlePaddle 框架）

```bash
# 访问 https://www.paddlepaddle.org.cn/install/quick 获取最新安装命令
# 例如 CUDA 11.2 版本：
python -m pip install paddlepaddle-gpu==2.5.2.post112 -f https://www.paddlepaddle.org.cn/whl/linux/mkl/avx/stable.html
```

### 4. 安装其他依赖

```bash
pip install numpy pygame
```

### 5. 安装 Redis（可选，用于分布式训练）

如果需要在多台机器上分布式训练，可以安装 Redis：

```bash
# Windows
# 下载 Redis for Windows 或使用 WSL

# Linux/Mac
sudo apt-get install redis-server  # Ubuntu/Debian
brew install redis  # Mac
```

### 6. 验证安装

运行环境检查脚本：
```bash
python check_env.py
```

确保：
- ✓ PyTorch 或 PaddlePaddle 已安装
- ✓ CUDA 可用（显示 `CUDA available: True`）
- ✓ NumPy 已安装
- ✓ Pygame 已安装（如果要用 GUI）

## 三、配置项目

### 1. 修改配置文件 `config.py`

打开 `config.py` 文件，根据你的环境进行配置：

```python
CONFIG = {
    'use_frame': 'pytorch',  # 或 'paddle'，根据你安装的框架选择
    'use_redis': False,  # 如果使用 Redis 分布式存储，设置为 True
    # ... 其他配置
}
```

**重要配置项说明：**
- `use_frame`: 选择 `'pytorch'` 或 `'paddle'`
- `use_redis`: 单机训练设为 `False`，分布式训练设为 `True`
- `play_out`: 每次移动的模拟次数（默认 1200，数值越大 AI 越强但速度越慢）
- `batch_size`: 训练批次大小（默认 512）
- `buffer_size`: 经验池大小（默认 100000）

## 四、运行项目

### 方式一：训练 AI（多进程同步训练）

这是项目的核心功能，用于训练象棋 AI。

**重要：确保已激活虚拟环境！**

#### 步骤 1：启动数据收集进程（可多开）

打开**多个终端窗口**，在每个终端中：

```bash
# 1. 进入项目目录
cd C:\CodeHome\aichess

# 2. 激活虚拟环境（如果使用虚拟环境）
source venv/Scripts/activate  # Git Bash
# 或
venv\Scripts\activate  # PowerShell/CMD

# 3. 运行数据收集
python collect.py
```

**说明：**
- 可以同时运行多个 `collect.py` 进程来加速数据收集
- 每个进程会进行自我对弈，生成训练数据
- 数据会保存到 `train_data_buffer.pkl` 文件中（或 Redis，如果启用）

#### 步骤 2：启动训练进程（只需一个）

打开**一个新的终端窗口**：

```bash
# 1. 进入项目目录
cd C:\CodeHome\aichess

# 2. 激活虚拟环境（如果使用虚拟环境）
source venv/Scripts/activate  # Git Bash
# 或
venv\Scripts\activate  # PowerShell/CMD

# 3. 运行训练
python train.py
```

**说明：**
- 只需要运行一个 `train.py` 进程
- 训练进程会读取收集的数据，更新神经网络模型
- 模型会保存为 `current_policy.pkl`（PyTorch）或 `current_policy.model`（PaddlePaddle）
- 每 100 个批次会保存一个检查点到 `models/` 目录

**训练流程：**
1. `collect.py` 进程不断进行自我对弈，生成数据
2. `train.py` 进程定期读取数据并训练模型
3. `collect.py` 进程会定期加载最新模型进行对弈
4. 循环迭代，AI 不断变强

### 方式二：人机对弈（测试训练好的模型）

#### 2.1 命令行版本

```bash
# 激活虚拟环境（如果使用）
cd C:\CodeHome\aichess
source venv/Scripts/activate  # Git Bash
python play_with_ai.py
```

**操作说明：**
- 按照提示输入走子（格式：如 "0304" 表示从 (0,3) 走到 (0,4)）
- AI 会自动走子

#### 2.2 GUI 图形界面版本（推荐）

```bash
# 激活虚拟环境（如果使用）
cd C:\CodeHome\aichess
source venv/Scripts/activate  # Git Bash
python UIplay.py
```

**操作说明：**
- 会打开一个图形界面窗口
- 点击棋子选择，再点击目标位置走子
- 支持背景音乐
- 可以设置 AI 的思考强度（修改 `n_playout` 参数）

**注意：** 运行 GUI 版本前，确保已安装 `pygame`：
```bash
pip install pygame
```

## 五、常见问题

### 1. 模型文件不存在

**问题：** 首次运行时会提示"已加载初始模型"，这是正常的。项目会从零开始训练。

### 2. GPU 不可用

**检查方法：**
```python
# PyTorch
import torch
print(torch.cuda.is_available())  # 应该返回 True

# PaddlePaddle
import paddle
print(paddle.device.is_compiled_with_cuda())  # 应该返回 True
```

**解决方法：**
- 确保安装了 GPU 版本的框架
- 确保 NVIDIA 驱动已正确安装
- 检查 CUDA 版本是否匹配

### 3. 数据文件被占用

**问题：** 多个进程同时访问 `train_data_buffer.pkl` 可能导致错误

**解决方法：**
- 使用 Redis（设置 `use_redis: True`）
- 或者确保只有一个 `collect.py` 进程在运行

### 4. 训练速度慢

**优化建议：**
- 增加 `collect.py` 进程数量（多开几个终端）
- 减少 `play_out` 参数值（但会降低 AI 强度）
- 确保使用 GPU 版本
- 使用 Redis 进行分布式训练

## 六、项目文件说明

| 文件 | 功能 |
|------|------|
| `collect.py` | 自我对弈数据收集（可多开） |
| `train.py` | 模型训练（只需一个） |
| `game.py` | 象棋游戏逻辑 |
| `mcts.py` | 蒙特卡洛树搜索 |
| `pytorch_net.py` | PyTorch 神经网络实现 |
| `paddle_net.py` | PaddlePaddle 神经网络实现 |
| `play_with_ai.py` | 命令行人机对弈 |
| `UIplay.py` | GUI 图形界面人机对弈 |
| `config.py` | 配置文件 |

## 七、快速开始示例

### 第一次运行（从零训练）

1. **创建并激活虚拟环境：**
```bash
cd C:\CodeHome\aichess
python -m venv venv
source venv/Scripts/activate  # Git Bash
# 或 venv\Scripts\activate  # PowerShell/CMD
```

2. **检查 CUDA 版本：**
```bash
python check_env.py
# 或运行 nvidia-smi 查看 CUDA 版本
```

3. **配置环境：**
```bash
# 根据你的 CUDA 版本安装 PyTorch GPU 版本
# CUDA 11.8:
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1:
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# 安装其他依赖
pip install numpy pygame
```

2. **修改配置：**
   - 打开 `config.py`，设置 `use_frame = 'pytorch'`

3. **启动训练：**
   - 终端 1：`python collect.py`
   - 终端 2：`python collect.py`（可选，多开加速）
   - 终端 3：`python train.py`

4. **等待训练：**
   - 训练需要较长时间，建议让程序运行数小时或数天
   - 模型会定期保存

5. **测试 AI：**
   - 训练一段时间后，运行 `python UIplay.py` 测试 AI 水平

## 八、参考资源

- B站视频：https://www.bilibili.com/video/BV183411g7GX
- 知乎文章：https://zhuanlan.zhihu.com/p/528824058
- AI Studio 项目：https://aistudio.baidu.com/aistudio/projectdetail/4215743

---

**提示：** 训练一个强大的 AI 需要大量时间和计算资源。建议在 GPU 服务器上运行，或者使用 AI Studio 的免费 V100 GPU 资源。

